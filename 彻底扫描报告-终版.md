# 资费监控 · 逐行全量扫描报告（终版）

> 日期：2026-09-12
> 方法：**逐行精读**，非抽样比对
> 范围：16 个 .py（逐行全文）+ 8 个 .js（关键段）+ 4 个 .yml（全文）+ 线上数据契约
> 共 11,696 行

---

## 〇、先修正我前几版的错误

| 前版说法 | 实际情况 | 性质 |
|---|---|---|
| v3："移动 history 无截断" | `build_site.py:464` 有 `history[-60:]` | **我说错了** |
| v2："广电多 3 字段是契约漂移" | 属实，但影响有限（前端兼容） | 保留 |
| v1/v2/v3 都是"扫一次多一个 bug" | 前三版用的是**结构比对**，这一版才逐行读 | 方法问题 |

**为什么越扫越多**：前三版在比"代码像不像"（相似度、重复度），
这种方法只能看到结构问题。真故障藏在单行逻辑里，
必须逐行读 + 推演运行时才能发现。

**本汇报的每个问题都附行号与验证方式。**

---

## 一、P0 致命（正在造成故障，必须立即修）

### 🔴 1. `fetcher.py:242` — `ua` 未定义，每次请求都抛 NameError

```python
def _post(opener, body_hex: str) -> str:      # ← 第230行：只有两个参数
    ...
    "User-Agent": ua or UA,                    # ← 第242行：ua 从哪来？
```

**证据**（AST 作用域分析）：
```
_post 参数: ['opener', 'body_hex']
_post 内使用但未定义: ['ua']      ← 确认
```

**最小复现**：
```python
UA = '浏览器A'
def _post(opener, body_hex): return ua or UA
_post(None, 'x')   # ❌ NameError: name 'ua' is not defined
```

**后果链**：
```
每次 _post → NameError
  → _fetch_cell 抛异常 → work() 捕获 → failed_keys +1
  → 6 个分类全失败 → items 为空 → 板块 0 条
  → has_net_error=True → 不判降级（所以看不到 DEGRADE 标记）
```

**这解释了今晚移动连跑 5 轮全部 0 条**——
不是源站限流，**是根本没发出过任何 HTTP 请求**。

**修法**（二选一）：
```python
# A. 加参数（推荐）
def _post(opener, body_hex, ua=None):
    ...
    "User-Agent": ua or UA,
# 调用处 fetcher.py:273 改为
plain = _post(opener, _encrypt(params), ua)

# B. 或不改签名，直接用全局 UA
"User-Agent": UA,      # _pick_ua() 已通过 global 更新过 UA
```

---

### 🔴 2. `peaks.json` 不在仓库 — 降级判定**从未真正生效**

```python
_PEAK_PATH = ".../pipelines/mobile/snapshots/peaks.json"   # fetcher.py:293
```

**验证**：仓库文件树中不存在 `peaks.json`，也不存在 `pipelines/mobile/snapshots/`。

**后果**：
- Actions 每轮是**全新 runner**（fresh checkout）
- `_load_peaks()` 每轮返回 `{}`
- `_evaluate_degrade()` 中 `_pk = []` → `_pt = 0` → `_conserved = False`
- `degraded = []` → `len(degraded) >= 2` 恒为假 → **confirmed 永远 False**

**即：整套"分类峰值降级判定"写了，但从未执行过一次。**

（这反而解释了为什么湖南的"降级"状态看起来诡异——
它根本不是峰值逻辑判出来的。）

**修法**：把 peaks.json 纳入版本管理（提交到仓库），
或改用页面内可持久化位置（如 `data/_peaks.json`，随站点数据一起提交）。

---

### 🔴 3. 电信：抓取失败时**先写空数据，后做 diff**

```python
items = fetch_scope(...)                              # :235 失败返回 []
data = {"scope": scope, "timestamp": now, "items": items}
save(os.path.join(out_dir, scope + ".json"), data)    # :239 ← 空数据已落盘！
...
r = diff_scope(prev_clean, cur)                       # :257 这时才 diff
if is_sampling_noise(r, _bt): ...                     # :259 护栏
r = mark_noise(r, _bt)                                # :261
```

**问题**：护栏只改写了 `history` 的记账，
**但 `:239` 已经把空 items 写进数据文件并推送到站点**。

**触发条件**：电信注释里明说"其余 29 省 provCode 无公开口径且
各省 h5 受瑞数 WAF 拦截"——**被 WAF 拦截就会走到这条路径**。

**后果**：电信湖南/全网数据被清空，站点显示 0 条。

**修法**：把 save 移到 diff 之后，且仅当 `items` 非空时才覆盖：
```python
if not items:
    print("本轮抓取为空，保留旧数据，不覆盖")
    continue          # 或直接 return，不写文件
```

---

### 🔴 4. `rm -rf` + `|| true`（三个 workflow 同款）

```bash
rm -rf unicom                                        # 先删
cp -a /tmp/art/unicom ./unicom 2>/dev/null || true   # 失败也继续
git add -A                                           # 把"删除"当变更提交
```

`cp` 失败（磁盘满 / /tmp 被清 / 权限）时，
**整个数据目录的删除会被当成正常变更提交**。

**修法**：先写后删，原子替换，失败即中止：
```bash
[ -d /tmp/art/unicom ] || { echo "产物缺失，中止"; exit 1; }
cp -a /tmp/art/unicom ./unicom.new \
  && rm -rf unicom && mv unicom.new unicom \
  || { echo "恢复失败，中止"; exit 1; }
```

---

## 二、P1 严重（造成数据不准 / 资源浪费）

### 🟠 5. 联通 `diff_scope` 返回字典里 `modified_details` 被赋值两次

```python
return {
    ...
    "modified_details": modified_details_for(pmap, modified),   # :328
    "added_details": ..., "removed_details": ...,
    "modified_details": dict(list(_mde.items())[:_DET_LIMIT]),  # :331 ← 覆盖前者
}
```

Python 字典字面量重复 key，**后者静默覆盖前者**。
`modified_details_for()` 的计算结果被丢弃。

**验证**：
```python
{'a':1, 'modified_details':'A', 'b':2, 'modified_details':'B'}
# → 'B'（A 被丢弃）
```

---

### 🟠 6. `_baseline.json` 写了，全仓无人读取

```
unicom_pipeline.py:391  update_baseline()  写入
unicom_pipeline.py:501 / :538              调用写入
全仓检索 _baseline                          ← 无任何读取处
```

**死数据**。每轮计算 md5 + titles 排序（32 板块 × 上千条），
**白白消耗 CPU 和 IO，没有任何作用**。

---

### 🟠 7. 联通 `fetch_pool` 中途失败 → 返回部分数据

```python
except Exception as e:
    print("    round %d 失败: %s" % ...)
    return seen          # :118 ← 返回已采集的部分
```

基线 1000 条，本轮网络中断只采到 300 条 → 直接返回 300 条
→ diff 记「下架 700」。虽有 `is_sampling_noise` 兜底，
但**依赖阈值（30%）**，恰好卡在阈值下方就会漏网。

---

### 🟠 8. history 截断策略四家不一致

| 管线 | 截断 | 位置 |
|---|---|---|
| 联通 | ✅ 30 条 + 3MB 兜底 | `:532` / `:48` |
| 移动 | ✅ 60 条 | `build_site.py:464` |
| 电信 | ❌ **无** | `:266-270` |
| 广电 | ❌ **无** | `:425-427` |

注释里写着"旧版累积到 42MB，前端需一次性下载"——
**同一个坑只堵了两家**。

---

### 🟠 9. 钉钉汇总时序错位（每天都在发生）

```
09:37 联通开始（耗时 ~70 分钟 → 10:47 结束）
10:00 钉钉汇总   ← 联通才跑 23/70 分钟
```

| 汇总时刻 | 读到的联通数据 | 滞后 |
|---|---|---|
| 10:00 | 昨天 21:37 那轮 | **~12 小时** |
| 20:00 | 今天 09:37 那轮 | ~9 小时 |

**用户以为看的是最新汇总，实际永远过期。**

---

## 三、P2 结构性问题

| # | 问题 | 数量/位置 | 影响 |
|---|---|---|---|
| 10 | 前端 5 份（2 份 MD5 完全相同） | app.js × 5 | 改一处漏一处 |
| 11 | `site/` 孤儿目录 | 4 文件 | 可能改错文件 |
| 12 | `os.getenv(k, default)` 空串坑 | 14 处 | 已踩坑 2 次 |
| 13 | 空 `catch {}` | 12 处 × 4 文件 | 错误静默 |
| 14 | 日志入库 | `fetch_run.log` `tg_run.log` | 仓库污染 |
| 15 | `print` 当日志 | 129 处 | 无法回溯 |
| 16 | latest.json 3 个生产者 | 各 pipeline | 已漂移（移动多 4 字段） |

---

## 四、执行方案（按此顺序）

### 第 1 批：止血（4 个 P0，改动都不大）

| 序 | 文件 | 改动 | 风险 |
|---|---|---|---|
| 1 | `mobile/fetcher.py:230,242,273` | 给 `_post` 加 `ua` 参数并传入 | 极低 |
| 2 | `mobile/fetcher.py:293` | peaks.json 改存 `data/_peaks.json` 并入库 | 低 |
| 3 | `telecom_pipeline.py:235-239` | 空结果时不写文件 | 低 |
| 4 | 三个 workflow | `rm -rf` 改原子替换 + 失败中止 | 低 |

**改完立刻验证**：手动触发一次移动抓取，
若各板块开始返回正常条数（非 0），即证明 P0-1 修复生效。

### 第 2 批：修数据准确性（5 个 P1）

| 序 | 文件 | 改动 |
|---|---|---|
| 5 | `unicom_pipeline.py:328/331` | 删除重复 key，保留其一 |
| 6 | `unicom_pipeline.py:391` | 删除 `update_baseline`（或改为真正被使用） |
| 7 | `unicom_pipeline.py:118` | 部分采集时标记，不参与 diff |
| 8 | 电信/广电 | 补 history 截断（对齐联通 30 条） |
| 9 | `dingtalk-summary.yml` | 改事件驱动（抓取完成触发） |

### 第 3 批：架构（可暂缓）

契约层 → 单一 guard → 三家 adapter → 前端模板化。

---

## 五、验证清单（改完必跑）

```
P0-1  fetcher 修复后：
      python3 -c "import fetcher; fetcher._post(None,'00')"
      → 不应再是 NameError（应为网络/解密错误）

P0-2  peaks 入库后：
      连续两轮运行，第二轮 _load_peaks() 应返回非空

P0-3  电信空数据保护：
      构造 fetch_scope 返回 [] → 数据文件不应被覆盖

P0-4  rm -rf 保护：
      模拟 cp 失败 → 脚本 exit 1，不提交删除

P1-5  联通 modified_details：
      检查输出中是否只有一个 modified_details 键
```

---

## 六、当前状态

四个 workflow 的 `schedule` 已注释（保留手动触发），重构期间不跑。
恢复：去掉 yml 中 `schedule:` 段的 `#`。

---

## 七、一句话总结

**今晚移动 5 轮全 0 条，不是源站限流，是 `fetcher.py:242` 的 `ua` 未定义
导致每一个 HTTP 请求都抛 NameError。**
修这一行，可能比前面所有重构都管用。
