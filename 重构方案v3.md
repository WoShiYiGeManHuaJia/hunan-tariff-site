# 资费监控项目 · 深度扫描 v3

> 更新：2026-09-12（第三次，逐行读关键逻辑而非只看结构）
> 本次方法：精读移动 fetcher.py 515 行、三个 workflow 全文、线上数据契约
> **v1/v2 找到的是"结构问题"，v3 找到的是"真故障源"**

---

## 一、v3 新发现：5 个会真实致故障的缺陷

前两版扫的是结构（重复度、耦合）。这次逐行读代码，
挖出 5 个**已经在造成故障**或**必然造成故障**的缺陷。

---

### 🔴 缺陷 1：峰值棘轮死锁（最严重，已在造成故障）

**位置**：`pipelines/mobile/fetcher.py` 第 484-489 行

```python
if confirmed:
    errors.insert(0, "DEGRADE:...")     # 判降级
else:
    for key, cur in cell_counts.items():
        if cur > peaks_sec.get(key, 0):  # ← 只增不减！
            peaks_sec[key] = cur
```

**问题**：峰值 `peaks[key]` **只增不减**。
判降级时走 `if confirmed` 分支，**峰值不更新**。

**后果链**（模拟验证过）：

```
源站真实业务调整，套餐从 1576 降到 97
  第1轮: 97/1576 = 6.2% < 60% → 判降级 → 跳过 → 峰值仍是 1576
  第2轮: 97/1576 = 6.2% < 60% → 判降级 → 跳过 → 峰值仍是 1576
  ...
  第N轮: 永远是 6.2% → 永远判降级 → 永远跳过
```

**峰值不会下降，所以永远出不来。没有自愈机制。**

这就是湖南从 09-09 卡到现在的真正原因——
不是"护栏逻辑错"，是**峰值被历史高点锁死，单向棘轮**。

**为什么 v2 的方案救不了**：v2 设计 3 的 `guard.decide()`
让"连续 3 轮接受新数据"，但**峰值不降，第 4 轮又会判降级**，
数据会在"接受 97"和"判降级"之间反复横跳。

**正确修法**：峰值必须可衰减
```python
# 接受新数据后，峰值向新值靠拢（衰减而非不变）
peaks[key] = max(cur, peaks[key] * DECAY)   # DECAY=0.8
# 或：连续 RESYNC 后直接重置峰值
if resync_count >= 3:
    peaks[key] = cur
```

---

### 🔴 缺陷 2：`rm -rf` + `|| true` = 数据目录可能整体消失

**位置**：三个 workflow 都有（unicom-fetch.yml 71-72 行等）

```bash
rm -rf unicom
cp -a /tmp/art/unicom ./unicom 2>/dev/null || true   # ← 失败也继续
git add -A                                            # ← 把"删除"提交上去
```

**问题**：先 `rm -rf` 删掉数据目录，再 `cp` 恢复。
**但 `cp` 失败时 `|| true` 让脚本继续**，接着 `git add -A`
会把"unicom 目录被删除"当成正常变更提交。

**触发条件**：磁盘满、/tmp 被清理、权限问题、cp 中断

**后果**：整个联通/电信/广电数据目录从仓库消失。
git 历史里还有，但当前分支数据全没了，所有用户看到 404。

**正确修法**：
```bash
# 先验证备份完整，再删；删前不做破坏性操作
[ -d /tmp/art/unicom ] || { echo "产物缺失，中止"; exit 1; }
cp -a /tmp/art/unicom ./unicom.new && rm -rf unicom && mv unicom.new unicom
```
即：**先写后删，用 mv 原子替换，失败即中止（不用 `|| true`）**。

---

### 🟠 缺陷 3：钉钉汇总时序错位（每天都在发生）

**cron 排期**：
```
09:37  联通开始（耗时 ~70 分钟 → 10:47 结束）
10:00  钉钉汇总   ← 联通才跑 23/70 分钟
```

**后果**：10:00 的钉钉汇总，**永远读不到联通本轮数据**，
读到的是昨天 21:37 那轮的结果。

| 汇总时间 | 联通数据实际是 | 滞后 |
|---|---|---|
| 10:00 | 昨天 21:37 那轮 | **~12 小时** |
| 20:00 | 今天 09:37 那轮（10:47完成） | ~9 小时 |

**用户以为看的是"最新汇总"，实际永远是过期的。**
这个问题每天都在发生，但从没报错——最隐蔽的一类。

**正确修法**：汇总改为**事件驱动**（抓取完成触发汇总），
而非固定 cron。或把联通排到汇总之前足够早。

---

### 🟠 缺陷 4：history 无截断，只有联通有

| 管线 | 是否截断 | 代码 |
|---|---|---|
| 联通 | ✅ 保留 30 条 | `history[-30:]` |
| 电信 | ❌ **无** | `history.append(entry)` 直接存 |
| 广电 | ❌ **无** | 同上 |
| 移动 | ❌ **无** | 未见截断 |

联通还有 `HIST_MAX_BYTES = 3MB` 兜底，其他三家**裸奔**。

**现状**（移动已经很明显了）：
```
data/          63.8 MB   history.json 3.4 MB  ← 手机全量下载
unicom/data    30.0 MB   history.json 234 KB
```

注释里写着"旧版累积到 42MB，前端需一次性下载"——
**同一个坑，只堵了联通一家。**

**后果**：电信/广电/移动的 history 无限增长，
前端加载越来越慢，最终手机端打不开。

---

### 🟡 缺陷 5：移动 data 目录 63.8 MB

```
移动  63.8 MB  (36 文件)   history.json 3.4 MB
联通  30.0 MB  (36 文件)
电信   1.8 MB  (4 文件)   ← 只有 4 个文件？
广电   3.0 MB  (35 文件)
```

移动是联通的 2 倍多。前端**全量下载**才能渲染。
（电信只有 4 个文件是因为它这两轮只抓到 2 个板块，
这是另一个问题——电信抓取基本失效。）

---

## 二、v1 / v2 / v3 诊断演进

| 版本 | 诊断 | 找到的问题 | 局限 |
|---|---|---|---|
| v1 | 代码重复 | 5 份前端、23 个重复函数 | 只看了"像不像" |
| v2 | 缺单一事实源 | latest 3 生产者、契约漂移 | 只看了"结构" |
| **v3** | **有状态逻辑错误** | **峰值棘轮、rm -rf、时序错位、无截断** | 需逐行读才可见 |

**v1/v2 的方法（看结构、比相似度）找不到 v3 的问题**——
因为这些问题代码"看起来很正常"，只有推演运行时行为才暴露。

---

## 三、修正后的重构方案（v3）

### 优先级重排：先修故障源，再做架构

**v2 建议先定契约再改代码。v3 修正：先止血，再定契约。**

| 优先级 | 内容 | 理由 |
|---|---|---|
| **P0** | 修峰值棘轮（可衰减 + 重置） | 湖南已冻结 3 天，**正在造成损失** |
| **P0** | 修 `rm -rf` + `\|\| true` | 数据消失风险，后果不可逆 |
| **P1** | 修钉钉时序（改事件驱动） | 每天都在给用户看过期数据 |
| **P1** | 给三家加 history 截断 | 移动已 3.4MB 且持续增长 |
| P2 | `core/config.py` 根治空串坑（14处） | 已踩坑 2 次 |
| P2 | 数据契约 + golden 测试 | 防漂移 |
| P3 | 单一 guard 决策入口 | 根治湖南类问题 |
| P3 | 三家 adapter 化 | 消除重复 |
| P4 | 前端模板化 + 健康面板 | 体验 |
| P0 | 删 `site/` 死代码（4 文件） | 零风险 |

### 关键设计修正

**v2 的 `guard.decide()` 不够**——它解决"决策分散"，
但解决不了"峰值锁死"。必须配合：

```python
def update_peak(old_peak, cur, accepted: bool):
    """峰值可衰减，避免单向棘轮死锁"""
    if cur >= old_peak:
        return cur                      # 创新高，直接更新
    if accepted:                        # 已被接受为新常态
        return max(cur, old_peak * 0.8) # 衰减 20%，逐轮逼近
    return old_peak                     # 未接受，保持
```

**加上 Health 字段记录 `last_ok_at`**，
健康面板就能显示"湖南已 3 天未成功"——
这个问题如果早有面板，第一天就能发现。

---

## 四、验证清单（重构后必须验证）

```
P0-1 峰值衰减：
  喂 (峰值1576, 本轮97) ×1 → HOLD，峰值仍 1576
  喂 (峰值1576, 本轮97) ×3 → RESYNC，峰值降为 ~1260
  喂 (峰值1260, 本轮97) ×N → 峰值逐轮下降，最终 97 被正常接受
  ✅ 不再有永久冻结

P0-2 rm -rf 保护：
  模拟 cp 失败 → 脚本必须 exit 1，不得提交删除
  ✅ 数据目录不消失

P1-1 钉钉时序：
  抓取完成 → 自动触发汇总
  ✅ 汇总读到的是本轮数据，非上一轮

P1-2 history 截断：
  连续写入 100 条 → 文件只保留最近 30 条
  ✅ 体积不再无限增长
```

**每个 P0/P1 都必须用构造数据本地验证，不靠线上跑。**
线上跑一轮 30-70 分钟，今晚就是这么耗掉几小时的。

---

## 五、当前状态

四个 workflow 的 `schedule` 已注释（保留手动触发），重构期间不跑。
恢复方法：去掉 yml 中 `schedule:` 段的 `#`。
