# 把「推送配置中心」装成 App

页面地址：
```
https://woshiyigemanhuajia.github.io/hunan-tariff-site/push-center.html
```

它已经是 **PWA（渐进式网页应用）**，可以不下载任何安装包，直接变成桌面上的一个 App 图标。

---

## 一、怎么装（1 分钟）

### Android（Chrome / Edge / 夸克等）

1. 手机浏览器打开上面的地址
2. 页面顶部会出现一条 **「📱 把本页装成 App」**，点右侧 **「添加到桌面」**
3. 弹窗点「添加 / 安装」

> 如果没看到引导条，走浏览器菜单：**⋮ → 添加到主屏幕 / 安装应用**

### iPhone / iPad（Safari）

1. 用 **Safari** 打开（其他浏览器不支持，iOS 限制）
2. 页面顶部提示：**点底部「分享」→ 选「添加到主屏幕」→ 添加**

> iOS 上这个操作必须由你手动完成，Safari 不开放给网页自动触发。

### 桌面（Chrome / Edge）

地址栏右侧会出现一个「安装」图标 ⊕，点一下即可。

---

## 二、装完是什么样

| 项目 | 说明 |
| --- | --- |
| 桌面图标 | 橙色底 + 白色铃铛，名称「推送配置」 |
| 打开方式 | 点图标进入，**没有地址栏**，看起来跟原生 App 一样 |
| 离线可用 | Service Worker 已缓存页面，断网也能打开 |
| 数据 | 密钥仍在 localStorage，**不会因此上传到任何服务器** |

---

## 三、PWA 和「真 APK」的区别

| | PWA（现在这个） | 真 APK |
| --- | --- | --- |
| 安装方式 | 浏览器添加，几秒 | 下载 apk，需允许「未知来源」 |
| 体积 | 约 30 KB | 通常 5–20 MB（要打包 WebView） |
| 更新 | 自动（刷新即最新） | 需重新下载安装 |
| 签名 | 不需要 | 需签名，否则装不上 |
| 后台定时推送 | ❌ 做不到 | ❌ 也做不到（Android 限制，需常驻服务） |
| 通知权限 | 需用户授权 | 需用户授权 |

**关键一点**：无论 PWA 还是 APK，**浏览器/网页关闭后都无法主动定时推送**——这是所有网页的安全机制，打包成 APK 也绕不过（Android 的 WebView 同样受限）。

所以定时推送仍然要靠：
- 路由器 / 电脑 / 服务器跑脚本（密钥完全不出本机）
- 或 GitHub Actions + Secrets（云端定时，Secrets 加密存储）

本 App 的定位是**「随时配置 + 随时手动推一次」**，跟定时推送是互补关系。

---

## 四、如果你确实想要 APK

两条路，都需要你自己操作（我这边没有 Android SDK，也没法给 APK 签名）：

1. **在线套壳**：搜索「网页打包 APK」，把上面的网址填进去，平台会生成一个壳 APK（内含 WebView 打开该地址）。注意选口碑好的，避免被注入广告。
2. **自己构建**：用 Capacitor 或 Cordova，把本仓库的 `push-center.html` + `manifest.json` + `sw.js` + `icons/` 放进 `www/`，本地 `gradle assembleRelease`。需要 Android Studio 环境。

两种方式的产物本质上还是这个网页，功能不会比 PWA 更多，只是多了个安装包形式。

---

## 五、文件说明

```
push-center.html   页面本体（单文件、零依赖）
manifest.json      App 名称 / 图标 / 启动方式
sw.js              离线缓存
icons/             192、512、apple-touch-180、favicon-32
```
