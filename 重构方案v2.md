# 资费监控项目 · 重构方案 v2

> 更新：2026-09-12（在 v1 基础上深度复扫）
> 扫描范围：28 个代码文件全文 + 线上 4 家数据契约逐字段比对
> 结论：v1 判断"问题是代码重复"——**不够准确**。真正的病根更深。

---

## 一、v1 漏掉的关键发现

v1 说"问题是代码重复"。复扫后发现，代码重复只是**症状**，
真正的病是下面三条。

### 发现 1：`latest.json` 有 3 个生产者，且**已经漂移**

三个 pipeline 各写一个 `build_latest()`：

| 管线 | 函数体行数 |
|---|---|
| 联通 | 31 行 |
| 电信 | **23 行** |
| 广电 | 27 行 |

**同一个数据产物，三份不同长度的实现。** 目前字段恰好对得上，纯属运气。

线上实际字段已经分裂：

```
移动 latest.json : 10 个字段（多 def_dist / dist / hunan_total / prov_dist）
联通/电信/广电   :  7 个字段
```

**移动比另外三家多 4 个字段。** 这不是设计，是各自演化出来的。

### 发现 2：数据契约漂移**已经发生**

对比四家 `history.json` 一条记录的字段：

```
联通 : added, removed, modified, added_names, removed_names, modified_names, modified_details
电信 : 同上
广电 : 同上 + added_list + removed_list + modified_list   ← 多三个
```

广电多出三个字段。现在前端靠"字段不存在就当空"侥幸兼容，
**任何一端改动都可能悄悄打破另一端，且不会报错。**

### 发现 3：状态散落在 4 处，互不统属

```
移动 peaks.json      → 抓取侧，判断降级
移动 snapshots/      → 抓取侧，上一版快照
联通 _baseline.json  → 管线侧
构建 _degrade.json   → 构建侧，降级计数
```

**判定所需的信息分散在四个地方**，任何一处不同步，
决策就是错的——这正是"湖南三天不更新"的成因：
抓取侧持有数据但不决策，构建侧想决策但没有数据。

---

## 二、真病根

### 不是"代码重复"，是**缺少单一事实源（SSOT）**

| 症状 | 表象 | 病根 |
|---|---|---|
| 湖南冻结三天 | 护栏逻辑错 | 状态散落 4 处，判定与数据分离 |
| 全网显示 0 条 | 构建脚本 bug | `latest.json` 有 3 个生产者 |
| 省份名空白 | 配置 bug | 空串覆盖默认值（14 处） |
| 结尾链接消失 | 配置 bug | 同上 |
| 数据白跑 68 分钟 | 并发 | git 提交三份拷贝 |
| 人工修正被覆盖 | 并发 | 同上 |

**每一条都能追溯到"同一件事有多份实现/多个真相"。**

---

## 三、v2 目标架构：契约优先

v1 的思路是"抽公共函数"。v2 换思路——**先定义契约，再让所有代码遵守**。

```
contract/          数据契约（唯一定义 + 版本 + 迁移 + 测试）
  schema.py        Item / Snapshot / Change / Health / Latest
  migrate.py       版本迁移 v1→v2
  tests/           golden file 契约测试

engine/            单一管线引擎（四家共用）
  fetch → parse → guard → diff → emit
  guard.py         唯一决策入口（打破 v1 描述的死循环）

adapters/          每家只保留差异
  unicom.py    fetch/parse（分页）
  telecom.py   fetch/parse（加密 + 无分页）
  gb.py        fetch/parse（分页）
  mobile.py    fetch/parse/adapt（name+fields → 统一契约）

site/              一套前端模板 + 四份配置
```

**核心原则变了**：
- v1：把重复代码抽出来
- v2：**每个数据产物只有一个生产者，每个决策只有一处实现**

---

## 四、四个关键设计

### 设计 1：数据契约（SSOT 的地基）

```python
@dataclass
class Item:
    id: str
    title: str
    fee: str
    category: tuple[str, str]   # (firstLevel, secondLevel)
    detail: dict
    fields: dict = {}           # 移动专用，其余为空

@dataclass
class Health:                   # 新增：数据自带健康状态
    ok: bool
    status: str                 # ACCEPT | HOLD | RESYNC | BASELINE | EMPTY
    count: int
    baseline: int
    degrade_rounds: int
    last_ok: str                # 上次成功时间
    error: str = ""

@dataclass
class Snapshot:                 # 一个板块一次抓取
    isp: str
    scope: str
    ts: str
    items: list[Item]
    health: Health              # 数据自带状态，不再散落他处

@dataclass
class Latest:                   # 仪表盘（唯一定义，唯一生产者）
    updated: str
    quanguo_total: int
    prov_total: int
    sections: list
    prov_stats: dict
```

**契约一旦定了：**
- 所有 adapter 输出 → 契约
- 所有前端消费 ← 契约
- 移动通过 `fields` 字段兼容，不用改结构
- 改动契约 → 契约测试立刻报警

### 设计 2：`Health` 字段 —— 可观测性（v1 完全没有）

**这是今晚最痛的教训。**

排查"湖南到底怎么了"花了 5 轮 × 30 分钟，原因是：
**没有任何地方记录"这个板块上次成功是什么时候、连续失败几轮"。**

`Health` 嵌进每份数据后，前端可以加一个**数据健康面板**：

```
板块健康度
  湖南    ⚠️ HOLD    上次成功 09-09 01:14（3天前）  连续 5 轮降级
  河南    ⚠️ HOLD    上次成功 09-09 01:13（3天前）  连续 5 轮
  广东    ✅ ACCEPT  上次成功 09-12 00:55
  ...
```

**如果早有这个面板，湖南的问题第一眼就能看见，不用查五轮。**

同时产出 `/health.json` 汇总，一眼看全站。

### 设计 3：单一决策入口 `guard.decide()`

v1 描述的死循环：

```
main.py 判降级 → 不写快照 → 构建侧无数据 → 决策不执行
```

v2 打破它：

```python
def decide(old: Snapshot|None, new: Snapshot, state: dict) -> Decision:
    """唯一决策入口。返回 action + 更新后的 state。"""
    if old is None:            return BASELINE   # 首次，建基线
    if new.count == 0:         return SKIP_EMPTY # 无数据，跳过（不吃掉基线）
    if new.count < old.count * DROP_RATIO:
        state['rounds'] += 1
        if state['rounds'] >= ACCEPT_ROUNDS:
            return RESYNC      # 连续降级达阈值，接受并重建基线
        return HOLD            # 保留旧数据，累计轮次
    state['rounds'] = 0
    return ACCEPT              # 正常
```

**关键**：`HOLD` / `RESYNC` 时**照常写快照**（带着 Health 状态），
数据不再卡在中间层。决策与数据同居一处。

### 设计 4：契约测试（防止再次漂移）

```
contract/tests/golden/
  input_unicom.json    → expected_output.json
  input_degrade.json   → expected_decision.json
```

改动后跑测试，**输出与 golden 不一致就失败**。

这次扫描发现的"广电多三个字段""移动多四个字段"，
如果有契约测试，当场就会被抓住，而不是靠我今天手动比对才发现。

---

## 五、额外发现：可以直接删的死代码

**`site/` 目录是孤儿**（4 个文件）

Pages 部署来源是 `main` 根目录。根 `index.html` 引用的也是根目录的
`app.js` / `style.css`。而 `site/` 下有一整套完整副本：

```
site/app.js            ← 与根 app.js 完全相同（MD5 一致）
site/common-extras.js  ← 与根完全相同
site/index.html
site/style.css
```

**四个文件，没有任何地方引用。** 可以直接删。

推测是某次整理时复制了整个站点，最后没用上。
留着的代价：每次改前端，可能改错文件（根目录那份才是生效的）。

---

## 六、分阶段计划（v2）

| 阶段 | 内容 | 收益 | 风险 | 顺序 |
|---|---|---|---|---|
| **0** | 删 `site/` 死代码 | ★★☆ | **零** | 立即 |
| 1 | `contract/schema.py` 数据契约 | ★★★★★ | 极低 | 先做 |
| 2 | `engine/guard.py` 单一决策 | ★★★★★ | 中 | 做 |
| 3 | `core/config.py` 根治空串坑（14处） | ★★★★★ | 极低 | 做 |
| 4 | `core/gitio.py` 统一提交 + 并发锁 | ★★★★☆ | 低 | 做 |
| 5 | 三家 adapter 化（保留各自 fetch/parse） | ★★★★☆ | 中 | 一家一家 |
| 6 | 移动 adapter 接入（只转契约，不改结构） | ★★★★☆ | 中 | 与 5 并行 |
| 7 | 契约测试 golden file | ★★★★☆ | 低 | 做完 5/6 后 |
| 8 | 前端一套模板 + 健康面板 | ★★★☆☆ | 中 | 最后 |

**阶段 0-4 是止血，5-7 是治本，8 是體驗。**

---

## 七、与 v1 的区别

| 维度 | v1 | v2 |
|---|---|---|
| 诊断 | 代码重复 | **缺少单一事实源** |
| 核心手段 | 抽公共函数 | **定义数据契约** |
| 移动 | 不合并，单独留 | **adapter 转契约**接入同一引擎 |
| 可观测性 | 无 | **Health 字段 + 健康面板** |
| 防漂移 | 无 | **golden file 契约测试** |
| 死代码 | 未发现 | **`site/` 4 个文件可删** |
| 决策 | 分散两层 | **单一 `guard.decide()`** |

---

## 八、验证方式（重要）

v2 的每个阶段都用**构造数据本地验证**，不靠线上跑：

```
阶段 2：喂 (基线1541, 本轮0)    → 期望 SKIP_EMPTY
        喂 (基线1588, 本轮97)×2 → 期望 HOLD
        喂 (基线1588, 本轮97)×3 → 期望 RESYNC
阶段 5：同一份联通数据 → adapter 输出必须等于 golden
```

**线上跑一轮 30-70 分钟，试错成本太高**（今晚就是这么耗掉的）。
本地验证几分钟一轮，可以反复迭代。

---

## 九、约束

- 移动管线**不强行改造结构**，只写 adapter 把 `name+fields` 映射为统一 `Item`
- 每阶段独立提交、可回滚
- 沙盒拦截 `*.github.io`，端到端需你在真机确认；我只能做逻辑层验证
- 不为"代码好看"做无收益重构

---

## 十、当前状态

四个 workflow 的 `schedule` 已注释（保留手动触发），重构期间不跑。
恢复方法：去掉 yml 中 `schedule:` 段的 `#`。
