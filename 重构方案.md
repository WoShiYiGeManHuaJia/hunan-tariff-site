# 资费监控项目 · 重构方案

> 扫描时间：2026-09-12
> 扫描范围：公开仓 `hunan-tariff-site` 全部 28 个代码文件（16 py + 8 js + 4 yml），177 个文件总量
> 说明：本文档只出方案，未改动任何代码。

---

## 一、扫描结果

### 1.1 代码资产分布

| 类型 | 数量 | 说明 |
|---|---|---|
| Python | 16 | 管线 / 构建 / 推送 |
| JavaScript | 8 | 四站前端 + PWA |
| Workflow | 4 | 移动 / 电信广电 / 联通 / 钉钉 |
| JSON（数据） | 114 | 四家运营商各省数据 |

其中纯代码约 **6 万行量级**（`app.js` 单文件 6.3 万字节 × 5 份）。

### 1.2 重复度（核心问题）

**前端：5 份近乎相同的前端**

| 文件 | 与其他的相似度 |
|---|---|
| `app.js` vs `site/app.js` | **完全相同**（MD5 一致） |
| `gb/app.js` vs `telecom/app.js` | 98.3% |
| `telecom/app.js` vs `unicom/app.js` | 96.6% |
| `gb/app.js` vs `unicom/app.js` | 95.6% |
| `common-extras.js` vs `site/common-extras.js` | **完全相同** |

**后端：23 个函数跨文件重复定义**

其中关键业务逻辑各写一套：

```
load          → 5 个文件各写一份
build_latest  → 3 个 pipeline 各写一份
diff_scope    → 3 个 pipeline 各写一份
digest_item   → 3 个 pipeline 各写一份
stat_section  → build_site / rebuild_latest 各一份
union_prov_dist → 同上
```

**git 提交重试逻辑：3 份拷贝**
`mobile-build.yml` / `telecom-gb-fetch.yml` / `unicom-fetch.yml` 各含 5 处 git 操作，
三份代码各自实现"备份→对齐→恢复→重试"。

### 1.3 脆弱模式统计

| 模式 | 数量 | 风险 |
|---|---|---|
| `os.getenv(k, default)` 空串坑 | **14 处** | 已实际踩坑 2 次 |
| `print` 当日志 | 129 处 | 出问题时无法回溯 |
| 硬编码 `time.sleep` | 16 处 | 重试策略不可控 |
| `except: pass` 静默失败 | 4 处 | 错误被吞掉 |
| `.get().get()` 链式取值 | 3 处 | 数据结构一变就崩 |
| 直接 `sys.exit` | 6 处 | 流程中断不可控 |

---

## 二、为什么三天两头出错

不是运气差，是三个结构性缺陷。

### 缺陷 1：配置解析不统一 → 空串覆盖默认值

**根因**：`os.getenv(k, default)` 的默认值**只在变量完全不存在时生效**。
但 workflow 里写的是 `${{ vars.XXX }}`，仓库没配这个 variable 时传进来的是**空字符串**，
空字符串会把 `default` 顶掉。

**已经踩了两次**：

| 时间 | 现象 | 根因 |
|---|---|---|
| 09-12 | 钉钉推送出现 ``◦ ``：无变化``（省份名为空） | `FOCUS_SEC=""` → 查表失败 |
| 09-12 | 钉钉推送结尾站点链接**整段消失** | `SITE_URL=""` → `if SITE_URL:` 为假 |

全仓还有 **14 处**同样写法，等于还有 14 颗雷。

### 缺陷 2：数据护栏逻辑分散在两层，形成死循环

这是"湖南/河南三天没更新"的真正原因：

```
main.py：板块被判降级 → 直接 continue，不写快照
                          ↓
              snapshots/ 一直是空的
                          ↓
build_site：没快照 → 走"无数据"防护 → 直接退出
                          ↓
      "连续 N 轮接受新数据"的逻辑永远走不到
                          ↓
              湖南永远冻结在旧数据
```

**判定的地方不持有数据，持有数据的地方不做判定。**

同类问题还有：`build_site.py` 和 `rebuild_latest.py` 各自实现一遍
`stat_section` / `union_prov_dist` / `prov_stats_map`，两份逻辑一旦不同步，
仪表盘数字就会对不上（已发生过一次"全网 0 条"）。

### 缺陷 3：git 提交三份拷贝 + 并发无协调

三份 workflow 各自写"备份→对齐→恢复→重试"，逻辑分散无法统一加固。

已发生的实际损失：

- 联通抓取 68 分钟**成功后提交失败**，数据白跑
- 人工修正的 history 记录**被并发任务覆盖回去**（修正 → 被覆盖 → 再修正）

---

## 三、目标架构

```
core/
  config.py   统一配置解析 —— 根治空串坑（一处修复，全仓生效）
  guard.py    统一数据护栏 —— 降级/骤降/0条/结构升级，单一判定入口
  gitio.py    统一 git 提交 —— 抗压冲突，唯一实现
  log.py      统一日志     —— 替代 129 处 print，可回溯
  store.py    统一读写     —— load/save/diff 唯一实现

pipelines/
  base.py     通用管线引擎（配置驱动：联通/电信/广电共用）
  unicom.py   仅保留差异化配置
  telecom.py
  gb.py
  mobile/     移动结构完全不同，单独保留（不强行合并）

site/
  template/   一套前端模板 + 四份配置
```

**核心原则**：每个决策只有**一处**实现。

---

## 四、分阶段方案

### 阶段 1：统一配置层 `core/config.py`

**改什么**
新增统一读取函数，空串 / 空白 / `None` 一律回退默认值：

```python
def env(key, default="", cast=str):
    v = (os.getenv(key, "") or "").strip()
    if not v:
        return default
    return cast(v)
```

替换全仓 14 处 `os.getenv` 危险写法。

**为什么先做这个**
这是唯一已经造成两次可见故障的根因，且修复成本最低、风险最小。

**验收**
脚本断言：环境变量设为空串时，返回默认值而非空串。

**回滚**
单文件新增，删除即回滚。

---

### 阶段 2：统一 git 提交 `core/gitio.py`

**改什么**
抽出唯一提交实现：

```
save_artifacts() → fetch → reset --hard → 恢复产物 → commit → push（重试 5 次）
```

三个 workflow 全部改为调用同一份脚本；同时加**全局并发锁**
（同一时刻只允许一个抓取任务提交）。

**为什么**
已造成两次实际数据损失（联通白跑 68 分钟、人工修正被覆盖）。

**验收**
两个任务并发跑，产物均不丢失、均成功提交。

**回滚**
workflow 恢复原内联脚本（git 历史可查）。

---

### 阶段 3：统一数据护栏 `core/guard.py`

**改什么**
把散落在 `main.py` / `build_site.py` / 三家 pipeline 的判定
收敛为单一入口，输出统一决策：

```python
decide(old_items, new_items, state) -> 
    ACCEPT      # 正常接受
    | HOLD      # 保留旧数据，累计轮次
    | RESYNC    # 连续降级达阈值，接受并重建基线
    | BASELINE  # 首次，仅建基线
    | SKIP_EMPTY # 本轮 0 条，跳过
```

**关键修复**：`HOLD` / `RESYNC` 时**照常写快照**，把"有没有数据、
有多少"交给下游判定，打破当前"不写快照 → 下游无数据 → 判定不执行"的死循环。

**为什么**
这是湖南/河南三天不更新的根因，也是最难发现的一类 bug。

**验收**
用构造数据跑 3 轮：第 1、2 轮 HOLD，第 3 轮 RESYNC。

**回滚**
`guard.py` 独立模块，旧逻辑保留一个版本开关。

---

### 阶段 4：统一日志 `core/log.py`

**改什么**
129 处 `print` 改为 `log.info/warn/error`，带时间戳与模块名，
关键决策（ACCEPT/HOLD/RESYNC）必打日志。

**为什么**
现在出问题只能靠猜。今晚排查"源站到底返回几条"时，
因为日志取不到，不得不临时加 `_diag.json` 落文件——这正是日志缺位的代价。

**验收**
任意一轮抓取的日志能回答：每个板块抓到几条、被哪条规则拦截。

---

### 阶段 5：三家 pipeline 合并为通用引擎

**改什么**
`unicom` / `telecom` / `gb` 三家结构相近，抽 `pipelines/base.py`，
各自只保留：接口地址、字段映射、省份表、解析函数。

**为什么**
`load` / `save` / `diff_scope` / `build_latest` / `digest_item`
五个函数在三家各写一遍，改一处要同步三处，必然漂移。

**风险**
三家虽粗似度只有 32-40%，但**关键函数签名一致**，可提取。
建议一家一家接入（联通 → 电信 → 广电），每家接入后单独验证。

**不做**
移动管线结构完全不同（加密 + 快照 + 峰值），**不强行合并**，
只让它接入阶段 1-4 的公共层。

---

### 阶段 6：前端模板化

**改什么**
5 份 app.js（含 2 份完全相同的）合并为 1 套模板 + 4 份配置。

**为什么**
今天改"变化历史不显示无变化省份"，同样的改动要在 4 个文件里做 4 遍。
漏一个就是不一致。

**优先级说明**
前端重复**不产生数据错误**，只增加维护成本。
对"三天两头出错"帮助最小，建议排最后。

---

### 阶段 7：移动管线接入公共层 + 限流治理

**改什么**
移动接入 `config` / `guard` / `gitio` / `log`，但保留独立抓取结构。
同时加**请求节流**：降低频率、失败退避（连续失败后指数拉长间隔）。

**为什么**
今晚移动连跑 5 轮全 0 条，疑似 IP 被源站限流。
现在的策略是"到点就跑，跑空也算"，会持续加重限流。

---

## 五、建议执行顺序

| 顺序 | 阶段 | 收益 | 风险 | 建议 |
|---|---|---|---|---|
| 1 | 统一配置 | ★★★★★ | 极低 | **先做** |
| 2 | 统一 git | ★★★★☆ | 低 | 做 |
| 3 | 统一护栏 | ★★★★★ | 中 | 做（需构造数据验证） |
| 4 | 统一日志 | ★★★☆☆ | 低 | 做 |
| 5 | pipeline 合并 | ★★★☆☆ | 中高 | 一家一家来 |
| 6 | 前端模板化 | ★★☆☆☆ | 中 | 最后做 |
| 7 | 移动 + 限流 | ★★★★☆ | 中 | 与 3 并行 |

**阶段 1-4 是止血，阶段 5-7 是治本。**

---

## 六、风险与约束

**回滚保障**
每个阶段独立提交，均有回滚路径（新增模块可直接删除，
改造模块可通过 git 历史还原）。

**验证方式**
阶段 3（护栏）和阶段 5（pipeline）必须用**构造数据**本地验证，
不能只靠线上跑——线上跑一轮 30-70 分钟，试错成本太高
（今晚就是这么耗掉的）。

**沙盒限制**
沙盒网关拦截 `*.github.io` 与部分外网 API，
端到端验证需要你在手机/真机确认，我这边只能做逻辑层验证。

**不做的事**
- 不强行合并移动管线（结构差异过大，合并弊大于利）
- 不为了"代码好看"做无收益的重构
- 不在同一阶段混合多个改动（无法定位问题时）

---

## 七、当前状态

四个 workflow 的 `schedule` 已全部注释（保留 `workflow_dispatch` 手动触发）。

恢复方法：把对应 yml 里 `schedule:` 段的 `#` 去掉即可。

```
.github/workflows/mobile-build.yml
.github/workflows/telecom-gb-fetch.yml
.github/workflows/unicom-fetch.yml
.github/workflows/dingtalk-summary.yml
```
